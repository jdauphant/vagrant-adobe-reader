vagrant-adobe-reader
====================

Vagrant that provide a fonctionnal adobe reader on linux

# Usage
    vagrant up
    # login with user vagrant password vagrant or do `vagrant reload`

# Ansible installation
    pip install ansible

# Dependancy 
- vagrant
- ansible
